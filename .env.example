# ============================================================
# Team Architect Assistant — Environment Variables
# Copy this file to .env and fill in the values.
# ============================================================

# ---- Azure OpenAI ----
AZURE_OPENAI_ENDPOINT=https://<resource>.openai.azure.com/
AZURE_OPENAI_API_KEY=<your-api-key>
AZURE_OPENAI_DEPLOYMENT=gpt-5.4-mini
AZURE_OPENAI_API_VERSION=2024-10-21

# ---- Entra ID (backend token validation) ----
ENTRA_TENANT_ID=<tenant-guid>
ENTRA_API_CLIENT_ID=<backend-app-registration-client-id>
ENTRA_API_AUDIENCE=api://<backend-app-registration-client-id>

# ---- KB Git repo ----
KB_REPO_URL=https://dev.azure.com/<org>/<project>/_git/team-kb
KB_REPO_BRANCH=main
KB_REPO_LOCAL_PATH=./kb_data
KB_REPO_AUTH_METHOD=pat
KB_REPO_PAT=<personal-access-token>
KB_SYNC_INTERVAL_SECONDS=900
KB_REPO_LOCAL_ONLY=true

# ---- Database ----
DATABASE_URL=sqlite:///./app.db

# ---- App ----
APP_ENV=dev
APP_LOG_LEVEL=INFO
APP_CORS_ORIGINS=http://localhost:5173

# ---- Auth bypass (dev only — rejected if APP_ENV != dev) ----
DEV_AUTH_BYPASS=true

# ---- Tool config ----
TOOL_SHELL_ENABLED=true
TOOL_AZ_CLI_ENABLED=true
TOOL_MS_DOCS_ENABLED=true
TOOL_APPROVAL_TIMEOUT_SECONDS=600

# ---- Rate limiting ----
CHAT_RATE_LIMIT_PER_MINUTE=30

# ---- Per-user weekly spend cap (DESIGN.md §5 2026-06-14) ----
# ENABLED shows the "credits left" indicator (soft); ENFORCED also blocks
# over-cap turns (hard). Both must be true to block. Caps are in USD; the UI
# shows credits (×100, 1 credit = $0.01) so 20.0 → 2000 credits. Set/clear
# per-user caps at /admin/users (architect-gated).
USAGE_CAP_ENABLED=true
USAGE_CAP_ENFORCED=false
USAGE_WEEKLY_CAP_USD_DEFAULT=20.0
# Real Azure prices, USD per 1M tokens, keyed by exact deployment name (single
# line). Layers over built-in placeholder defaults — set for accurate credits.
# USAGE_PRICE_TABLE_JSON={"gpt-5.4":{"prompt":2.50,"cached":0.25,"completion":10.00},"gpt-4o-mini":{"prompt":0.15,"cached":0.075,"completion":0.60}}
# Optional per-role caps (USD); a per-user value wins over these.
# USAGE_ROLE_CAPS_JSON={"architect":50,"engineer":25}

# ---- Backup (prod only) ----
BACKUP_ENABLED=false
BACKUP_AZURE_STORAGE_CONNECTION_STRING=
BACKUP_CONTAINER_NAME=sqlite-backups
BACKUP_INTERVAL_SECONDS=86400
