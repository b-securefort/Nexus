fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
sqlmodel>=0.0.22
alembic>=1.14.0
openai>=1.50.0
gitpython>=3.1.43
msal>=1.31.0
pydantic-settings>=2.5.0
python-json-logger>=2.0.7
prometheus-client>=0.21.0
httpx>=0.27.0
ddgs>=9.0.0
trafilatura>=2.0.0
lxml_html_clean>=0.4.0
pyyaml>=6.0.2
aiosqlite>=0.20.0
PyJWT>=2.9.0
cryptography>=43.0.0
diagrams>=0.24.0

# KB hybrid retrieval (Phase 2)
# Embeddings via Azure OpenAI text-embedding-3-small — no local ONNX models needed.
sqlite-vec>=0.1.9
numpy>=1.26.0
pypdf>=5.0.0

# Role-based access via Azure App Configuration (Entra App Roles → tools mapping).
# Read once at startup; falls back to hardcoded defaults in app/auth/rbac.py if
# unreachable. Managed Identity (DefaultAzureCredential) is used to authenticate.
azure-appconfiguration>=1.5.0
azure-identity>=1.19.0
